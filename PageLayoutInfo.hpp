#ifndef PAGELAYOUTINFO_HPP
#define PAGELAYOUTINFO_HPP

namespace SongBook
{
    const int TABLE_ROW_COUNT = 18;
    const int N_SONG_PER_ROW = 2; // we use 3 column to print a song so we don't name this variable a TABLE_COL_COUNT
}

#endif
